mutation createDevice($createDeviceDevice:deviceCreateInput!){
    createDevice(device: $createDeviceDevice){
        id,
        createdAt,
        devicekey,
        region,
        hardware,
        description
    }
}
